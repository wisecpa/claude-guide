# Claude 활용 가이드 (내부용)

이 저장소의 `index.html`은 **암호화된 페이지**입니다 (StatiCrypt, AES-256).
접속하면 비밀번호 입력 화면이 나오며, 사무실 공용 비밀번호로 열람합니다.

- 원본(평문)은 관리자 PC의 `src/` 폴더에서만 관리합니다 (저장소에 올리지 않음)
- 내용 수정: 관리자 PC에서 `src/index.html`의 `SITE_DATA` 수정 → `배포.bat` 더블클릭
- 비밀번호 변경: `암호설정.bat` 실행 후 `배포.bat`으로 재배포
